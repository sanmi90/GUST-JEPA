# ============================================================================
# PREVENT Project - Python Dependencies
# ============================================================================

# Core scientific stack
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter & interactive computing
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.0.0
ipython>=8.0.0
ipdb>=0.13.0

# PyTorch
# vortex-jepa training, smoke, and benchmark runs MUST use the RTX 6000
# Blackwell (sm_120) GPU (CLAUDE.md "Hardware", HANDOFF.md D19). These
# pins ship cu130 wheels that include sm_120 kernels. Install with the
# default PyPI index (download.pytorch.org/whl/cu128 also works but its
# CDN was unreachable from the workstation at the time of the upgrade).
#     pip install --upgrade torch torchvision torchaudio
# The src/utils/device.py require_rtx6000() helper probes a tiny kernel
# at startup and raises NoRTX6000Error with a clear remediation message
# if the wheel does not actually run on the Blackwell card.
torch==2.12.0
torchvision==0.27.0
torchaudio==2.11.0

# Machine learning & signal processing
scikit-learn>=1.3.0
h5py>=3.9.0

# Development & testing
black>=23.0.0
flake8>=6.0.0
pytest>=7.4.0
pytest-cov>=4.1.0

# Experiment tracking & monitoring
wandb>=0.15.0
tensorboard>=2.14.0

# Utilities
einops>=0.7.0
PyYAML>=6.0
tqdm>=4.66.0

