"""Loss kit for the Session 31 canonical v2.2 retrain.

The kit is the single loss menu over which every model is a switch-set. The
autoencoder and JEPA models share one code path and differ only by which terms
are turned on in their config (the fairness keystone, SESSION 31 Track A).
"""
