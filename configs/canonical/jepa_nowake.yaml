# Canonical: predictive JEPA, lift only, no wake supervision.
# Track C row jepa_nowake (predictive, lift only; approx old ctrl_pred_vit_nowake).
name: jepa_nowake

representation_objective:
  pred: {on: true}

anti_collapse:
  on: true
  method: sigreg

supervision:
  lift_head: {on: true}
  wake_head: {on: false}
