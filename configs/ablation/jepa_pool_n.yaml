# Track C cell CN (Session 34): pooled JEPA, near-body lift-element head ONLY.
# Q2-alt: is the Chang near-body head a superior lift conditioning to scalar L?
name: jepa_pool_n

representation_objective:
  pred: {on: true}

anti_collapse:
  on: true
  method: sigreg

supervision:
  lift_head: {on: false}
  wake_head: {on: false}
  nearbody_head: {on: true}

model:
  encoder: cnn_vit
  latent: pooled
