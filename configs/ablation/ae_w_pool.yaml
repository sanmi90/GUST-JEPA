# Track C anchor AE-W (Session 34): pooled matched AE, wake head ONLY.
# Objective-axis anchor for the CW cell (recon vs pred at identical wake-only
# conditioning); completes the spec-exact AE-L / AE-W anchor pair.
name: ae_w_pool

representation_objective:
  recon: {on: true}

anti_collapse:
  on: true
  method: sigreg

supervision:
  lift_head: {on: false}
  wake_head: {on: true}
  nearbody_head: {on: false}

model:
  encoder: cnn_vit
  latent: pooled
