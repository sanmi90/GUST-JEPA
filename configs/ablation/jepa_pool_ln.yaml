# Track C cell CLN (Session 34): pooled JEPA, lift + near-body heads.
# Interaction cell of the full 2x2x2 conditioning cube.
name: jepa_pool_ln

representation_objective:
  pred: {on: true}

anti_collapse:
  on: true
  method: sigreg

supervision:
  lift_head: {on: true}
  wake_head: {on: false}
  nearbody_head: {on: true}

model:
  encoder: cnn_vit
  latent: pooled
