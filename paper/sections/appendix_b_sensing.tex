% Appendix: sparse wall-pressure state estimation, redone on the v2.1
% unconditioned rebuild. Placement is the target-blind qDEIM optimum of the
% pressure modes (so the cross-family ordering cannot be a latent-aware-placement
% artefact); the comparison is which representation, estimated from sparse wall
% pressure, best recovers the latent STATE and the flow FIELD. The central honest
% point is that latent recoverability and physical decode quality are different
% axes: the state-recovery winner is not the field-recovery winner, because the
% field score also carries the decoder's reconstruction ceiling. The earlier
% pressure-to-wake-enstrophy claim is reported only as a non-result.

At deployment the vorticity field is not available; only the wall pressure is. We
ask how much of the latent state, the gust parameters, and the recovered flow field
can be read from a sparse set of $K$ wall-pressure taps over a pre-impact window,
and through which representation. The input is the span-averaged pressure on the
$192$ surface taps; the pressure-to-latent map is a kernel-ridge regressor with a
radial-basis kernel on the flattened $K$-sensor by pre-impact-window feature
vector, fitted on the training encounters and applied unchanged on the held-out
set. The window has $W = 30$ frames ending at the readout instant (impact plus
$H = 16$, the closure-matrix primary endpoint), so the pressure is available up to
but not beyond the readout. This is a feasibility comparison across
representations, not a closed controller.

\subsubsection*{Target-blind placement}
The placement we use throughout is qDEIM, the QR-pivot optimum of the wall-pressure
modes \citep{manohar2018sensors}. It is target-blind and family-independent: it
places taps to reconstruct the pressure field, with no knowledge of the latent it
is later asked to recover, and the same eight taps are used for every
representation. The cross-family recoverability ordering of
\S\ref{sec:res_observability} therefore cannot be an artefact of a bespoke
latent-aware sensor selection. A target-aware greedy selection (target-conditioned
sensor importance, which adds at each step the tap that most improves recovery of
the leading latent component from the already-selected taps) is also available, and
its only measurable advantage over qDEIM is in the scarce-sensor regime; by $K = 8$
the wall pressure is information-rich enough that the placement choice no longer
separates the families, which is why we report the target-blind optimum.

\subsubsection*{State recovery and its anisotropy}
With $K = 8$ taps the held-out test\_b state $R^2$ orders the families as in the
main text (\S\ref{sec:res_observability}): predictive $\SenseStateJepaTf$,
reconstructive autoencoder $\SenseStateFukami$, variational baseline
$\SenseStateBvae$, linear basis $\SenseStatePod$. The variance-weighted $R^2$ is
reported alongside the mean canonical correlation between the estimated and true
latent, because the two expose different things. The linear basis has the highest
canonical correlation ($\SenseCcPod$ against $\SenseCcJepaTf$ for the predictive)
yet the lowest state $R^2$: the wall pressure aligns with many POD directions but
those are largely the low-variance ones, so a high directional agreement
recovers little of the state's energy. The predictive latent has the lower
canonical correlation but recovers the high-variance directions that carry the
state, which is the quantity a downstream probe reads.

\subsubsection*{Recovered field is a different axis from recovered state}
Decoding the pressure-estimated latent through each family's frozen decoder gives a
recovered flow field (figure~\ref{fig:flow_recovery}). Scored by structural
similarity, the predictive latent reaches $\FieldPrSsimJepaTf$, the linear basis
$\FieldPrSsimPod$, and the reconstructive $\FieldPrSsimFukami$: the predictive
latent beats the reconstructive but ties the linear basis, and the tie is not
significant (one-sided sign test $p = \FieldPrSsimSignP$). The tie is a decoder
property, not a state-recovery one. The decode ceiling, the similarity of the field
decoded from the \emph{true} encoded latent with no pressure step, is highest for
the linear basis ($\FieldCeilSsimPod$ against $\FieldCeilSsimJepaTf$ for the
predictive and $\FieldCeilSsimFukami$ for the reconstructive), so the linear basis
reaches field parity through its decoder's reconstruction ceiling while having the
weakest state recovery of the three. The pressure-to-state gap, the ceiling minus
the pressure-recovered similarity, is correspondingly largest for the linear basis
($\FieldSsimGapPod$ against $\FieldSsimGapJepaTf$ for the predictive): the part of
the field error attributable to imperfect pressure-to-state recovery is smallest
for the predictive latent. The volume relative error and peak-vorticity retention
agree with the similarity ordering at the impingement scale (predictive
$\FieldPrEpsJepaTf$ relative error and $\FieldPrPeakJepaTf$ peak retention, against
$\FieldPrPeakPod$ for the linear basis). The state-recovery winner is therefore not
the field-recovery winner ($\FieldStateWinnerMatches$ of the two orderings
coincide), and the clean comparison is the state recovery, where the predictive
latent is unambiguously the most recoverable.

\begin{figure}
  \centering
  \includegraphics[width=\linewidth]{sections/figures/results/figG_flow_recovery_v2p1.pdf}
  \caption{Flow recovered from $K = 8$ target-blind (qDEIM) wall-pressure taps on a
  representative held-out encounter (the typical-difficulty case
  $G = +1.50$, $D = 1.50$, $Y = +0.10$), one row per family. Columns: the field
  decoded from the pressure-estimated latent, the decode ceiling (the same decoder
  applied to the true encoded latent, with no pressure step), and the simulation.
  The per-panel similarity makes the appendix point visible: the linear basis
  reaches field parity through the highest decode ceiling, not through better
  pressure-to-state recovery, where the predictive latent has the smallest ceiling
  gap. The decoder is blurry by design (\S\ref{sec:res_physical}); the comparison
  is between the columns of the same row.}
  \label{fig:flow_recovery}
\end{figure}

\subsubsection*{Parameters and the out-of-distribution boundary}
The gust parameters recover directly from the same eight taps, with the
wall-normal offset the weakest ($Y$ at $R^2 = \SensePY$ on test\_b), consistent
with $Y$'s weak resolution throughout the paper and confirming that the limit is
the mid-plane vorticity observable and the offset sampling, not the pressure
signal. The impact lift read through the latent ($R^2 = \SensePClVia$) is no better
than reading it straight off the pressure, in keeping with the main-text point that
the wall reads the scalar lift well but the wake structure only through the
representation. On the out-of-distribution test\_c set ($|G| = 4$) every estimator
and every family gives negative state $R^2$, the same three-dimensional
observability boundary of \S\ref{sec:disc_limits} that bounds the rest of the
model.

\subsubsection*{Robustness and a non-result}
The frame-0 gust-release clip of the re-release encounters leaves the result
unchanged: removing the clipped frame from the pressure window shifts the
predictive state recovery by $\SenseCausalDelta$ in $R^2$, within noise. We do not
claim wake recovery from the wall: a pressure-to-latent-to-frozen-wake-probe route
reaches only $R^2 = \SensePWakeJepaTf$ for the predictive latent, a direct
pressure-to-wake regression $\SensePWakeDirect$, and the predictive advantage over
the best reconstructive family is not significant (one-sided sign test
$p = \SensePWakeSignP$). The wake is linearly readable from the encoded field
(\S\ref{sec:res_closure}) but not from this many wall taps over this window; the
deployment comparison is the state and the field, not the wake.
