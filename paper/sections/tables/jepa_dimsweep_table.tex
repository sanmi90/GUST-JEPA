% Auto-generated by scripts/session23/build_jepa_table.py. Do not edit by hand.
\begin{table}
  \centering
  \caption{Conditioned-model control: latent-dimension sweep and the two
  head/conditioning ablations for the predictive (JEPA) family, at $H = 16$ on the
  $n = 42$ test\_b encounters, under the matched predictor and probes of
  \S\ref{sec:res_controls}. The first rows condition the predictor on $\cvec=(G,D,Y)$
  and serve as the conditioned reference for the unconditioned model the paper
  reports (the ``no $\cvec$'' row, last). ``Repr.\ wake MAE'' is the
  representational mean absolute error of the wake enstrophy from the
  simulation-encoded latent (smaller is better); the $R^2$ columns are the forecast
  coefficient of determination of the Markov rollout (larger is better, the
  predict-the-mean floor is $0$); ``mean $R^2$'' averages the six observables. The
  ``wake only'' row removes the instantaneous-lift head at matched $d = 64$ (the
  complement of the wake-removal control in table~\ref{tab:controls_2x2}); the
  ``no $\cvec$'' row removes the gust-parameter conditioning from the predictor at
  matched $d = 64$, so the model never sees $(G,D,Y)$ and matches the unconditioned
  configuration used throughout the paper. Removing the conditioning leaves the
  representation almost intact (repr.\ wake MAE $34.21$ against $29.83$, the encoder
  being unconditional in both) and degrades the in-distribution forecast gracefully.}
  \label{tab:jepa_dimsweep}
  \small
  \fittab{\begin{tabular}{l l c c c c}
    \toprule
    $d$ & Configuration & Repr.\ wake MAE & $C_L$ $R^2$ & wake $R^2$ & mean $R^2$ \\
    \midrule
    64 & lift $+$ wake & 29.83 & 0.723 & 0.449 & 0.445 \\
    32 & lift $+$ wake & 37.99 & 0.751 & 0.214 & 0.362 \\
    16 & lift $+$ wake & 50.09 & 0.642 & 0.265 & 0.363 \\
    8 & lift $+$ wake & 45.61 & 0.467 & -0.072 & 0.174 \\
    4 & lift $+$ wake & 58.51 & 0.181 & 0.194 & 0.137 \\
    64 & wake only & 35.44 & 0.542 & 0.313 & 0.276 \\
    64 & lift $+$ wake, no $\cvec$ & 34.21 & 0.392 & 0.038 & 0.309 \\
    \bottomrule
  \end{tabular}}
\end{table}
