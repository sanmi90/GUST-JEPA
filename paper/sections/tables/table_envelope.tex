% Table V (v3): the gust-intensity operating envelope. tab:envelope.
% Numbers are macros_v3.tex macros (outputs/session33/numbers.json).
\begin{table}
  \centering
  \caption{The operating envelope, by gust strength, for the predictive coefficient
  state under the base frozen filter, used here as a diagnostic. Median analysis lift closure,
  median analysis lift error in lift units,
  and consistency-failure rate of the filter (the fixed innovation-consistency
  criterion of \S\ref{sec:methods_lae}, not a load-tracking failure) against the
  static single-frame recovery, over all encounters (train,
  \ValSplit, \TestSplit, and the $|G| = 4$ \BoundarySplit{} sets), stratified by $|G|$. The
  $|G| \le 0.5$ closure is low because the true lift is near-constant there and
  the $R^2$ denominator vanishes, not because the filter fails; the per-encounter
  paired tests on the \TestSplit{} and \BoundarySplit{} sets are in Appendix~\ref{app:reg}.}
  \label{tab:envelope}
  \small
  \fittab{\begin{tabular}{l c c c c}
    \toprule
    $|G|$ & filter $C_L$ $R^2$ & filter $C_L$ RMSE & consistency-failure rate & static recovery $C_L$ $R^2$ \\
    \midrule
    $\le 0.5$ & $\VfiltCLGHalf$    & $\VfiltCLrmseGHalf$    & $\VdivGHalf$    & $\VrecCLGHalf$ \\
    $1$       & $\VfiltCLGOne$     & $\VfiltCLrmseGOne$     & $\VdivGOne$     & $\VrecCLGOne$ \\
    $1.5$     & $\VfiltCLGOneHalf$ & $\VfiltCLrmseGOneHalf$ & $\VdivGOneHalf$ & $\VrecCLGOneHalf$ \\
    $2$       & $\VfiltCLGTwo$     & $\VfiltCLrmseGTwo$     & $\VdivGTwo$     & $\VrecCLGTwo$ \\
    $3$       & $\VfiltCLGThree$   & $\VfiltCLrmseGThree$   & $\VdivGThree$   & $\VrecCLGThree$ \\
    $4$       & $\VfiltCLGFour$    & $\VfiltCLrmseGFour$    & $\VdivGFour$    & $\VrecCLGFour$ \\
    \bottomrule
  \end{tabular}}
\end{table}
