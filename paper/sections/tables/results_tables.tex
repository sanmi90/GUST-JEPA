% Tables for the Results section. Only the two tables discussed at the very top
% of Section 4 (the families overview and the headline closure table) live here
% and are \input at the start of the section. The later tables (conditioning
% floor, latent drift, 2x2 controls) are placed inline next to their first
% reference, further down the section, so floats land near their discussion.

% ---------------------------------------------------------------------------
% TABLE: model families and objectives (new; standard JFM summary table).
% ---------------------------------------------------------------------------
\begin{table}
  \centering
  \caption{Encoder families compared under the matched-predictor protocol of
  \S\ref{sec:methods_protocol}. The model is fully unconditioned: no gust
  parameters enter the encoder or the predictor. All three families share one
  autoregressive transformer predictor, training recipe, and probe family; only
  the encoder differs. ``Aux.\ heads'' lists the observable supervision attached
  to the encoder. The architecture and auxiliary-head columns differ across
  families, which is why the headline result is attributed to the predictive
  \emph{family} and isolated only by the controls of \S\ref{sec:res_controls}.}
  \label{tab:families}
  \small
  \fittab{\begin{tabular}{l l l l l}
    \toprule
    Family & Objective & Encoder & Latent dim.\ $d$ & Aux.\ heads \\
    \midrule
    JEPA (this work) & predictive (latent) & CNN $+$ ViT & $16,\,32,\,64$ & lift $+$ wake \\
    Reconstructive AE & reconstructive (field) & CNN & $3,\,16,\,32,\,64$ & lift \\
    POD & linear projection & none & $16,\,32,\,64$ & none \\
    \bottomrule
  \end{tabular}}
\end{table}

% ---------------------------------------------------------------------------
% TABLE: held-out closure (test_b MAE). THIS IS THE HEADLINE TABLE.
% ---------------------------------------------------------------------------
\begin{table}
  \centering
  \caption{Held-out closure of the unconditioned model at $H = 16$ on the
  $n = 42$ test\_b encounters, in two modes. \emph{(a)} Representational closure,
  the headline: the coefficient of determination $R^2$ when a fixed linear probe
  is applied to the simulation-encoded latent (the latent encoded directly from
  the held-out field), larger is better, with the per-observable mean absolute
  error of the predictive latent given alongside in native units. \emph{(b)}
  Forecast closure, reported as confirmatory: the $R^2$ of the Markov rollout from
  the impact frame against the simulation observable (the variance baseline is the
  held-out split mean, so $R^2 < 0$ is worse than predicting that mean). The
  predictive entry is the fully unconditioned model (tf-no-c, $d = 64$); the
  reconstructive (Fukami) and linear (POD) wake $R^2$ are each family's least-bad
  value across $d$, to be maximally fair. The representation is where the families
  separate: the unconditioned predictive latent encodes the wake enstrophy at
  $R^2 = 0.71$, essentially the conditioned model's $0.75$ (\S\ref{sec:res_controls}),
  while the reconstructive and linear latents are at $0.06$ and below. The forecast
  panel points the same way and is read as secondary; the per-encounter paired test
  is in table~\ref{tab:paired_closure}, the latent-dimension sweep and the
  conditioned reference in table~\ref{tab:jepa_dimsweep}, and full probe values with
  bootstrap CIs in Appendix~\ref{app:reg}.}
  \label{tab:closure}
  \small
  \fittab{\begin{tabular}{l c c c c c c c}
    \multicolumn{8}{c}{\textit{(a) Representational closure: held-out $R^2$ (and predictive-latent MAE, native units)}}\\
    \toprule
    & $C_L$ & $C_D$ & $I_y$ & wake-enst.\ & circ.\ pos.\ & circ.\ neg.\ & mean \\
    \midrule
    predictive (uncond.), $d{=}64$ & \textbf{0.88} & \textbf{0.69} & $-0.86$ & \textbf{0.71} & \textbf{0.78} & \textbf{0.85} & \textbf{0.51} \\
    \quad MAE & 0.50 & 0.25 & 2.68 & 35.1 & 0.79 & 0.76 & \\
    \midrule
    reconstructive (Fukami), wake $R^2$ & & & & 0.06 & & & \\
    linear (POD), wake $R^2$ & & & & $-0.17$ & & & \\
    \bottomrule
  \end{tabular}}

  \vspace{6pt}

  \fittab{\begin{tabular}{l c c c c c c c}
    \multicolumn{8}{c}{\textit{(b) Forecast closure (secondary): coefficient of determination $R^2$ of the Markov rollout}}\\
    \toprule
    & $C_L$ & $C_D$ & $I_y$ & wake-enst.\ & circ.\ pos.\ & circ.\ neg.\ & mean \\
    \midrule
    predictive (uncond.), $d{=}64$ & 0.60 & 0.35 & $-0.39$ & \textbf{0.56} & \textbf{0.24} & \textbf{0.79} & \textbf{0.36} \\
    reconstructive (Fukami), $d{=}64$ & \textbf{0.73} & 0.41 & \textbf{0.04} & $-0.48$ & 0.18 & 0.00 & 0.15 \\
    linear (POD), $d{=}64$ & 0.68 & \textbf{0.54} & $-0.81$ & $-0.09$ & 0.06 & 0.51 & 0.15 \\
    \bottomrule
  \end{tabular}}
\end{table}
