% Tables for the Results section. Only the two tables discussed at the very top
% of Section 4 (the families overview and the headline closure table) live here
% and are \input at the start of the section. The later tables (conditioning
% floor, latent drift, 2x2 controls) are placed inline next to their first
% reference, further down the section, so floats land near their discussion.

% ---------------------------------------------------------------------------
% TABLE: model families and objectives (new; standard JFM summary table).
% ---------------------------------------------------------------------------
\begin{table}
  \centering
  \caption{Encoder families compared under the matched-predictor protocol of
  \S\ref{sec:methods_protocol}. The model takes no gust parameters: none enter the
  encoder or the predictor. All three families share one
  autoregressive transformer predictor, training recipe, and probe family; only
  the encoder differs. ``Aux.\ heads'' lists the observable supervision attached
  to the encoder. The architecture and auxiliary-head columns differ across
  families, which is why the headline result is attributed to the predictive
  \emph{family} and isolated only by the controls of \S\ref{sec:res_controls}.}
  \label{tab:families}
  \small
  \fittab{\begin{tabular}{l l l l l}
    \toprule
    Family & Objective & Encoder & Latent dim.\ $d$ & Aux.\ heads \\
    \midrule
    JEPA (this work) & predictive (latent) & CNN $+$ ViT & $16,\,32,\,64$ & lift $+$ wake \\
    Reconstructive AE & reconstructive (field) & CNN & $3,\,16,\,32,\,64$ & lift \\
    POD & linear projection & none & $16,\,32,\,64$ & none \\
    \bottomrule
  \end{tabular}}
\end{table}

% ---------------------------------------------------------------------------
% TABLE: held-out closure (test_b MAE). THIS IS THE HEADLINE TABLE.
% ---------------------------------------------------------------------------
\begin{table}
  \centering
  \caption{Held-out closure at $H = 16$ on the $n = 42$ test\_b encounters, at
  matched $d = 64$, in two modes. \emph{(a)} Representational closure, the headline:
  the coefficient of determination $R^2$ when a fixed linear probe is applied to the
  simulation-encoded latent (the latent encoded directly from the held-out field),
  larger is better. \emph{(b)} Forecast closure, reported as confirmatory: the $R^2$
  of the full-context rollout against the simulation observable (the variance
  baseline is the held-out split mean, so $R^2 < 0$ is worse than predicting that
  mean). The representation is where the families separate: the predictive latent
  renders the wake enstrophy linearly readable ($R^2 = \NumReprWakeJepaTf$) where the
  reconstructive (Fukami) and linear (POD) latents are at or below zero. The forecast
  panel points the same way and is read as secondary; the per-encounter paired test
  is in table~\ref{tab:paired_closure} and full probe values with bootstrap CIs in
  Appendix~\ref{app:reg}. The $\beta$-VAE representation row uses the matched-recipe
  encoder; its single forecast row is rolled out from the faithful-recipe predictor,
  the only $\beta$-VAE rollout available, and is read accordingly.}
  \label{tab:closure}
  \small
  \fittab{\begin{tabular}{l c c c c c}
    \multicolumn{6}{c}{\textit{(a) Representational closure: held-out $R^2$}}\\
    \toprule
    & $C_L$ & $C_D$ & wake-enst.\ & circ.\ pos.\ & circ.\ neg.\ \\
    \midrule
    predictive (JEPA) & \textbf{\NumCloReprCLJepaTf} & \textbf{\NumCloReprCDJepaTf} & \textbf{\NumReprWakeJepaTf} & \textbf{\NumCloReprCircPosJepaTf} & \textbf{\NumCloReprCircNegJepaTf} \\
    reconstructive (Fukami) & \NumCloReprCLFukami & \NumCloReprCDFukami & $\NumReprWakeFukami$ & \NumCloReprCircPosFukami & \NumCloReprCircNegFukami \\
    $\beta$-VAE & \NumCloReprCLBvae & \NumCloReprCDBvae & $\NumReprWakeBvaeMatch$ & \NumCloReprCircPosBvae & \NumCloReprCircNegBvae \\
    linear (POD) & \NumCloReprCLPod & \NumCloReprCDPod & $\NumReprWakePod$ & $\NumCloReprCircPosPod$ & \NumCloReprCircNegPod \\
    \bottomrule
  \end{tabular}}

  \vspace{6pt}

  \fittab{\begin{tabular}{l c c c c c}
    \multicolumn{6}{c}{\textit{(b) Forecast closure (secondary): full-context rollout $R^2$}}\\
    \toprule
    & $C_L$ & $C_D$ & wake-enst.\ & circ.\ pos.\ & circ.\ neg.\ \\
    \midrule
    predictive (JEPA) & \NumCloFcstCLJepaTf & \NumCloFcstCDJepaTf & \textbf{\NumCloFcstWakeJepaTf} & \NumCloFcstCircPosJepaTf & \textbf{\NumCloFcstCircNegJepaTf} \\
    reconstructive (Fukami) & \NumCloFcstCLFukami & \NumCloFcstCDFukami & $\NumCloFcstWakeFukami$ & \NumCloFcstCircPosFukami & \NumCloFcstCircNegFukami \\
    $\beta$-VAE & \NumCloFcstCLBvae & \NumCloFcstCDBvae & $\NumCloFcstWakeBvae$ & \NumCloFcstCircPosBvae & \NumCloFcstCircNegBvae \\
    linear (POD) & \textbf{\NumCloFcstCLPod} & \textbf{\NumCloFcstCDPod} & $\NumCloFcstWakePod$ & $\NumCloFcstCircPosPod$ & \NumCloFcstCircNegPod \\
    \bottomrule
  \end{tabular}}
\end{table}
