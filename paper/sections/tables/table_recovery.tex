% Table W (v3): wall-pressure recovery. tab:recovery.
% Numbers are macros_v3.tex macros (outputs/session33/numbers.json).
\begin{table}
  \centering
  \caption{\rb{K}{TREC-C1}{What eight sensors recover of each family's state, with the per-family estimator selection disclosed. The exhibit behind the physics-against-variance ordering.}What eight wall-pressure sensors recover of each family's coefficient
  state, on the \TestSplit{} set, with the recovery estimator selected by cross-validation per
  family and the observables read through the frozen probes. Raw-variance
  recoverability and physics recoverability disagree: the reconstruction latent is
  the most recoverable in state variance but the least in physics, while the
  predictive latent carries the most wall-recoverable physics from a less
  recoverable state. The absolute state recovery is estimator-limited: a per-family
  tuned LSTM (hidden width validation-selected up to $512$) recovers more from the
  same sensors, raising the predictive state to $\WtStateJepa$ and the reconstruction
  to $\WtStateFukami$, and it had not saturated at the largest network tested, so
  these are a lower bound on the wall-recoverable state; the family ordering, which
  is the claim, is unchanged.\re}
  \label{tab:recovery}
  \small
  \fittab{\begin{tabular}{l l c c}
    \toprule
    State & recovery estimator & state $R^2$ & observable $R^2$ \\
    \midrule
    predictive             & \MakeUppercase{\WpickJepa}   & $\WstateJepa$   & $\WobsJepa$ \\
    reconstructive (Fukami) & \MakeUppercase{\WpickFukami} & $\WstateFukami$ & $\WobsFukami$ \\
    linear (POD)           & \MakeUppercase{\WpickPod}    & $\WstatePod$    & $\WobsPod$ \\
    \bottomrule
  \end{tabular}}
\end{table}
