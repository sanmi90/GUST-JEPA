# SESSION 36: JFM rewrite, phase 1 (audit, ledgers, M1/M2 compute, nomenclature)

Governing spec for the session. Branch `jfm-rewrite-v2` off `session35-manuscript-v4`.
This spec merges the two externally prepared documents in `editorial/upstream/`
(`claude_code_jfm_rewrite_v2.md` stages 0-2 and `SESSION_MS_manuscript_compute.md`
tracks M1-M2) into one dependency-ordered session, per the packaging decision of
2026-07-10 (Carlos, this session; see HANDOFF D266). Stages 3-6 and Track M3 are
Sessions 37-38 work and are out of scope here except as flags.

The upstream master prompt's rules apply verbatim throughout (numbers.json to macros
as single source of truth, never invent or silently alter a number, comment protocol
`% REVIEW-NUMBER:` / `% REVIEW-CLAIM:` / `% FIGURE-TODO:` / `% PROVENANCE-TODO:`,
British spelling, no em-dashes, no test-set-justified selection statements, split
aliases, physical G sign, one commit per stage).

## 0. Mission and session-close gates

Mission: end the session at the NUMBERS-FROZEN GATE, after which every number,
macro, nomenclature token and claim disposition is stable, so Sessions 37-38 are
pure prose and figures.

Close gates (all must hold):
1. `latexmk -pdf main` rc=0; zero undefined refs/citations (check_refs.py clean).
2. `scripts/session35/trace_numbers.py` at exactly the whitelisted hits.
3. Stage 0 audit + Stage 1 ledgers approved by Carlos (STOP 1, STOP 2).
4. Track M1 complete: shared-operator merit for every families-table row, 3 seeds,
   case-clustered CIs; gate branch recorded as D310; table disposition applied.
5. Track M2 complete: every PROVENANCE.md item dispositioned
   (kept / updated / disclosed-v2.1); D311/D312 recorded.
6. Stage 2 complete: zero stray old nomenclature outside figure assets and the
   appendix legend; zero em-dashes in touched prose.
7. SESSION36_REPORT.md written; HANDOFF and CLAUDE.md current focus updated; pushed.

## 1. Locked decisions (Carlos, 2026-07-10)

- L1 Packaging: one combined session in this environment (the workstation).
  The uploaded two-session split is superseded.
- L2 Concordance: `paper/main.pdf` IS the exported main_25.pdf. The memo's
  figure/table numbering maps 1:1 to the current build; Stage 0 verifies rather
  than infers the mapping.
- L3 D301 RESOLVED: recompute the families-table forecast-merit column under the
  shared direct forecaster (Track M1). The strong/weak/null gate auto-resolves
  the table disposition; no further author input needed.
- L4 D303/D306 RESOLVED: adopt `editorial/upstream/front_matter_rewrite.tex`.
  Title becomes "Predictive reduced-order states for wall-pressure estimation of
  extreme vortex gust--airfoil interactions". The drafts bind in Session 37
  Stage 4 with numbers bound to macros; recorded in HANDOFF now.
- L5 HANDOFF numbering: D301-D319 reserved as the manuscript-rewrite block so the
  upstream documents' cross-references stay valid; organic decisions continue at
  D267+.

Still open (in-flight stubs, non-blocking this session): D302 (fig-21-slim
placement; Session 38 Stage 5 STOP), D304 (per-D half-divergence gust ratios;
Carlos supplies values; `% REVIEW-NUMBER` flag until then), D305
(calibration-audit placement; Session 38). Carlos-owned and untouched: session35
branch merge, DNS Table 1, Zenodo DOI, license/CRediT/funding.

## 2. Adaptation table (upstream spec to actual repo)

| Upstream | Actual |
|---|---|
| `sections/` | `paper/sections/` (+ `paper/sections/v4/`, `paper/sections/tables/`) |
| build `latexmk -pdf main` | same, run in `paper/`; bib `refs.bib` + `refs_to_add.bib`, style `jfm.bst` |
| `numbers.json` | `outputs/session33/numbers.json` (active; `_provenance` block, per-entry source/split/fmt) |
| macros | `paper/macros_v3.tex` GENERATED by `scripts/session33/emit_macros_v3.py`; `paper/macros.tex` is legacy session28 output, marked non-live in the audit; neither is hand-edited |
| new numbers | new part JSON in `outputs/session33/numbers_parts/` (session-35 emitter pattern) -> eval/emit chain -> macros; nothing hand-typed |
| audit scripts | `scripts/session36/{audit_numbers,lint_language,check_refs,wordcount}.py`; `audit_numbers.py` wraps `scripts/session35/trace_numbers.py` + whitelist, never forks it |
| ledgers | `editorial/` at repo root |
| nomenclature macros | NEW hand-maintained `paper/nomenclature.tex`, input from `main.tex`, separate from generated macro files |
| `figstyle.py` | `scripts/session21/figstyle.py`; assets `paper/sections/figures/{results,tikz}/` |
| M1 operator | `scripts/session34/latent_rex.py` hardcodes width 256 / 3 quantiles; the manuscript-selected operator is LSTM width 512 / 9 quantiles / AdamW 1e-3 / 6000 iterations (s3.2.1, `paper/sections/v4/s3_3_rex.tex`; selection in `scripts/session34/rex_tune.py`). M1 uses a parameterised `scripts/session36/rex_families_m1.py` pinned to that config |
| "ten table-6 families" | pinned at Stage 0 from the actual families-table rows (`paper/sections/tables/table_baselines.tex`, `table_family_filter.tex`), not from `da_dims_grid.json`; latent inputs from `outputs/session34/trackc_latents/` |
| M2 item list | seeded from the memo but classified at Stage 1 against EXISTING v2p2 artifacts first (`scripts/session33/{spectrum_dmd_v2p2,wake_code_v2p2,topology_v2p2}.py` outputs, `outputs/session33/track_p3_mechanism.json`, `outputs/session33/track_t_recovery_grid.json`); expected outcome mostly confirm-and-retag |
| GPU runs | `require_rtx6000`, `OMP_NUM_THREADS<=8`, `taskset -c 0-15`, W&B group `partition_v2p2` + the mandatory traceability keys |

## 3. Phase order and gates

### Phase 0 (commit 1): setup
Branch (done), `editorial/upstream/` docs (done), track the stray PNG twin
`fig_phi_panel_v4.png` (repo convention tracks pdf+png pairs), this spec, HANDOFF
D266 + reserved block with L3/L4 recorded resolved and D302/D304/D305/D310-D312
as open stubs.

### Phase 1 (commit 2, STOP 1): Stage 0 audit
1. Baseline: build rc=0, snapshot `paper/build/baseline.pdf` (gitignored), tracer
   at the expected single M5 sentinel hit, latexmk warnings recorded.
2. Create the four audit scripts under `scripts/session36/` (upstream specs,
   paths adapted; wordcount budgets keyed to the actual section files).
3. `editorial/MANUSCRIPT_AUDIT.md`: per-section word counts vs budgets
   (abstract 250, s1 1300, s2 1600, s3 2600, s4.1 1300, s4.2 1200, s4.3 1000,
   s4.4 1800, s5 1200, s6 450); figure/table inventory with the memo-numbering
   concordance VERIFIED against main.pdf; the memo's 15 catches located at
   file:line; the 9 mandatory Stage 4 claim repairs located; acronym inventory;
   banned-language and em-dash hits; carried session-35 items (M5 sentinel,
   caption seed-provenance audit, POD-vs-AE intro tension, sign-convention
   placement) with dispositions.
4. Resolve the M5 sentinel (archived-commit citation for the 0.2 strong-effect
   bar at its current tracer-flagged location, or drop per cite-or-drop).
5. STOP. Show the audit; wait for approval before any scientific prose changes.

### Phase 2 (commit 3, STOP 2): Stage 1 ledgers
1. `editorial/CLAIM_MAP.md`: the four primary claims (state construction;
   compression; common-operator forecasting; wall-pressure estimation), each
   with exact figure/table/equation and split; unsupported or multiply-stated
   claims marked, not repaired.
2. `editorial/PROVENANCE.md`: every main-text number and figure tagged v2.2 or
   v2.1; each M2 target classified confirm-and-retag vs re-run;
   `% PROVENANCE-TODO` on anything untraceable.
3. STOP. Show both ledgers (may be reviewed together with STOP 1).

### Phase 3 (commits 4-5): Tracks M1 + M2, overlapped with Phase 4
M1: `rex_families_m1.py` fits the pinned operator identically on every pinned
family x 3 operator seeds on frozen cached latents; five-observable mean merit
at H=16 on the in-distribution test split through the frozen probes
(`src/evaluation/rollout.py::fit_observable_probes`); case-clustered bootstrap
CIs (2000 resamples on encounter means). Outputs `outputs/session36/`; W&B group
`partition_v2p2`, tag MS-M1; keys `merit_shared_<family>_{mean,lo,hi}` in a new
numbers part. Gate (records D310):
- Strong (ordering matches suited-operator, top-block CIs disjoint): replace the
  merit column; prose unchanged except the operator description.
- Weak (ordering preserved, CIs overlap): replace column; ordering stated as
  directional, mirroring the existing seed-variance caution.
- Null (ordering changes): shared-operator column becomes the primary result,
  suited-operator table moves to supplementary, `% REVIEW-CLAIM` reword items
  queued for Session 37.

M2: re-run only the outside-CI or untraceable items from PROVENANCE.md.
Within-CI: keep the quoted value, update the provenance tag. Outside-CI: update
the numbers part, regenerate macros through the emit chain, `% REVIEW-CLAIM` if
any sentence's direction changes. Deliberately retained v2.1 numbers get the
table-13-pattern disclosure. Records D311 (per-item dispositions) and D312 (any
claim rewordings).

### Phase 4 (commit 6): Stage 2 nomenclature, while M1 trains
`paper/nomenclature.tex`: `\PredState` (wake-supervised predictive state, was
CLW/flagship), `\LiftState` (lift-focused predictive state, was CLN/specialist),
`\DirectFC` (direct multi-horizon forecaster, was REX/TiRex), `\FnoiseKF`
(forecast-noise filter), `\TwoStageKF` (two-stage filter), `\LinLatKF` (linear
latent filter, was LAE-KF), `\StaticInv` (static delay-embedded inverse),
`\ValSplit` / `\TestSplit` / `\BoundarySplit` (validation / in-distribution test
/ boundary test). Mechanical migration across all sections and captions; cube
codes survive only on the cube-figure axes plus one table legend row; JEPA
confined to the s1 and s3 lineage discussion; "leakage-free" replaced by the one
definitional sentence in s3.3 plus "the pressure-only estimator" thereafter.
Build + tracer + check_refs green; grep-verified zero stray old names.

### Session close (STOP 3): numbers-frozen gate
Rerun all four audit scripts + build + tracer; SESSION36_REPORT.md; HANDOFF
outcomes; CLAUDE.md current focus; push.

## 4. Roadmap after this session

- Session 37: Stage 3 restructure (the v4 `s4_{a,b,c,d}` subfiles already mirror
  the target 4.1-4.4 parts, so this is promotion-and-prune) + Stage 4 prose
  (word budgets, language table, 9 claim repairs, front matter binds per L4;
  STOP at front matter).
- Session 38: Stage 5 figures (Track M3 redraws through figstyle, target 11-12
  main figures / 5 tables) + Stage 6 consistency (NUMBER_AUDIT.md, visual diff
  vs baseline) + freeze. D302/D305 decided at the Stage 5 STOP. Fold into 37 if
  figures go fast.
